<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allen.swayakt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwayaKT">
        <activity android:name=".Disease"></activity>
        <activity android:name=".Info" />
        <activity android:name=".PredictActivity" />
        <activity android:name=".Karanupdated" />
        <activity android:name=".Karan" />
        <activity android:name=".Adarsh" />
        <activity android:name=".Ananth" />
        <activity android:name=".About" />
        <activity android:name=".MainPage1" />
        <activity android:name=".Mainpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>